# Markdown 基础语法

## 标题
使用 `#` 符号表示标题，级别从1到6：

```markdown
# 一级标题
## 二级标题
### 三级标题
#### 四级标题
##### 五级标题
###### 六级标题
```

## 文本格式

- **粗体**：`**文本**` 或 `__文本__`
- *斜体*：`*文本*` 或 `_文本_`
- ***粗斜体***：`***文本***`
- ~~删除线~~：`~~文本~~`
- `行内代码`：`` `代码` ``

## 列表

### 无序列表
使用 `-`、`+` 或 `*`：
```markdown
- 项目1
- 项目2
  - 子项目2.1
  - 子项目2.2
```

### 有序列表
使用数字加点：
```markdown
1. 第一项
2. 第二项
3. 第三项
```

## 链接和图片

- 链接：`[链接文本](URL)`
- 图片：`![替代文本](图片URL)`

示例：
```markdown
[百度](https://www.baidu.com)
![示例图片](https://example.com/image.png)
```

## 引用

使用 `>` 符号：
```markdown
> 这是一段引用文本
> 可以多行
>> 嵌套引用
```

## 代码块

使用三个反引号，可指定语言：

````markdown
```csharp
public class Program
{
    static void Main(string[] args)
    {
        Console.WriteLine("Hello World!");
    }
}
```
````

## 表格

```markdown
| 列1 | 列2 | 列3 |
|-----|-----|-----|
| 数据1 | 数据2 | 数据3 |
| 数据4 | 数据5 | 数据6 |
```
对齐方式：
- 左对齐：`|:---|`
- 居中：`|:---:|`
- 右对齐：`|---:|`

## 分割线

使用三个或更多的 `-`、`*` 或 `_`：
```markdown
---
***
___
```

## 转义字符

使用反斜杠 `\` 转义特殊字符：
```markdown
\* 不会变成斜体
\# 不会变成标题
```

## 任务列表

```markdown
- [x] 已完成任务
- [ ] 未完成任务
- [ ] 待处理任务
```

这些是最常用的 Markdown 语法，足以应对大部分文档编写需求。