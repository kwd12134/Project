<UserControl
    x:Class="MachineVision.Defect.Views.DefectEditView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:MachineVision.Shared.Controls;assembly=MachineVision.Shared"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MachineVision.Defect.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:i ="http://schemas.microsoft.com/xaml/behaviors"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition />
            </Grid.RowDefinitions>

            <StackPanel Margin="15" Orientation="Horizontal">
                <TextBlock FontSize="22" Text="当前项目:" FontWeight="Bold"/>
                <TextBlock Text="{Binding Model.Name}" FontSize="22" Margin="10,0"/>
            </StackPanel>

            <StackPanel
                Grid.Row="1"
                Margin="15"
                Orientation="Horizontal">
                <Button Command="{Binding LoadImageCommand}" Content="导入图像" />
            </StackPanel>

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <ListBox x:Name="ListBox" ItemsSource="{Binding Files}" Foreground="Black" SelectionChanged="ListBox_SelectionChanged">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding FileName}" />
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                <controls:ImageEditView Grid.Column="1" Image="{Binding Image}"/>
            </Grid>
        </Grid>

    </Grid>
</UserControl>
