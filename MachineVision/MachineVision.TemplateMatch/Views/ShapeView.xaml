<UserControl
    x:Class="MachineVision.TemplateMatch.Views.ShapeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MachineVision.TemplateMatch.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
    d:DesignHeight="450"
    d:DesignWidth="800"
    Background="{DynamicResource MaterialDesignPaper}"
    TextElement.Foreground="{DynamicResource MaterialDesignBody}"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <StackPanel Margin="15">
            <TextBlock FontSize="22" Text="find_shape_model" />
            <TextBlock Text="Find the best matches of a shape model in iamge" />
        </StackPanel>

        <TabControl Grid.Row="1" Foreground="Black">
            <TabItem Header="参数说明" />
            <TabItem Header="关联算子" />
            <TabItem Header="代码示例" />
            <TabItem Header="开始使用" />
        </TabControl>

    </Grid>
</UserControl>
